apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: haproxy
  name: inventory
spec:
  rules:
  - host: inventory.inventory-src.10.19.2.21.nip.io
    http:
      paths:
      - backend:
          service:
            name: inventory
            port:
              number: 8080
        pathType: Prefix
        path: /
