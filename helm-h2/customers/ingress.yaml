apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: haproxy
  name: customers
spec:
  rules:
  - host: customers.fedev.10.19.2.21.nip.io
    http:
      paths:
      - backend:
          service:
            name: customers
            port:
              number: 8080
        pathType: Prefix
        path: /
